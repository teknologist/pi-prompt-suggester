/**
 * TODO: optional standalone stale-check entrypoint.
 *
 * Intended usage:
 * - run staleness check manually
 * - print trigger payload when stale
 */
export async function runStaleCheckScript(): Promise<void> {
	throw new Error("Not implemented: runStaleCheckScript");
}
